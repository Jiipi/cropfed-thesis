# Decision log

Tài liệu này ghi các quyết định ảnh hưởng đề tài. Trạng thái `Accepted` chỉ thay đổi bằng một entry mới, không xóa lịch sử.

| ID | Ngày | Trạng thái | Quyết định | Lý do chính |
|---|---|---|---|---|
| D-001 | 2026-07-30 | Accepted | Giữ nguyên tuyệt đối tên đề tài chính thức | yêu cầu hành chính của đồ án |
| D-002 | 2026-07-30 | Accepted | “Phát hiện” = classification ở mức ảnh trong MVP | phù hợp dữ liệu/nguồn gần bài toán; object detection cần annotation khác |
| D-003 | 2026-07-30 | Accepted | Dùng subset cà chua PlantVillage 10 lớp | scope vừa sức, có healthy/disease/pest |
| D-004 | 2026-07-30 | Accepted | Bốn client cross-silo mô phỏng | đại diện cơ sở, đủ quan sát heterogeneity, phù hợp đồ án |
| D-005 | 2026-07-30 | Accepted | IID + Dirichlet alpha 0.5/0.1 | có baseline và hai mức label skew |
| D-006 | 2026-07-30 | Accepted | MobileNetV2 là model chính | cân bằng compute/communication; có transfer learning |
| D-007 | 2026-07-30 | Accepted | Bắt buộc local-only, centralized, FedAvg, FedProx | trả lời đầy đủ giá trị hợp tác và ảnh hưởng Non-IID |
| D-008 | 2026-07-30 | Accepted | Flower Message API >=1.32,<2 | tài liệu hiện hành; tránh bắt đầu bằng legacy API |
| D-009 | 2026-07-30 | Accepted | FastAPI + React + PostgreSQL; image training ở runner riêng | tách control plane khỏi compute dài |
| D-010 | 2026-07-30 | Accepted | Tách global test trước client partition | ngăn leakage và giữ comparison công bằng |
| D-011 | 2026-07-30 | Accepted | Macro F1 là metric chính | class imbalance; accuracy không đủ |
| D-012 | 2026-07-30 | Accepted | Smoke NumPy phải có nhãn `synthetic_smoke_only` | kiểm tra code nhưng ngăn nhầm thành kết quả nghiên cứu |
| D-013 | 2026-07-30 | Accepted | Raw images không nằm trong source/bàn giao | license, kích thước và privacy boundary |
| D-014 | 2026-07-30 | Accepted | FL không được mô tả là bảo đảm privacy hoàn chỉnh | update/model vẫn có nguy cơ rò rỉ |
| D-015 | 2026-07-30 | Accepted | Dùng RQ1–RQ5 cho lõi MVP; RQ6 về mô hình nhẹ chỉ kết luận đầy đủ khi có backbone đối chiếu | giữ đúng mục tiêu nghiên cứu nhưng không biến giả thuyết MobileNetV2 thành kết luận thiếu đối chứng |
| D-016 | 2026-07-30 | Accepted | Checkpoint dùng envelope có format/model version, class order, metadata và checksum; loader vẫn hỗ trợ raw state dict cũ | ngăn nhầm taxonomy/model, tăng truy vết và không phá checkpoint 0.1.0 |
| D-017 | 2026-07-30 | Accepted | Trên Windows, dùng virtualenv Python 3.12 riêng cho Flower/Ray simulation đã kiểm chứng; môi trường lõi vẫn có thể dùng Python 3.13 | Ray 2.55.1 có runtime Windows tương thích với Python 3.12 trong môi trường kiểm chứng, còn Python 3.13 không cài được simulation extra đầy đủ |
| D-018 | 2026-07-30 | Accepted | Flower integration smoke phải lưu log, checkpoint, checksum và tự xác nhận 4/4 client; artifact ảnh tổng hợp luôn mang `research_result_valid=false` | tạo bằng chứng end-to-end có thể kiểm tra nhưng ngăn nhầm smoke thành kết quả nghiên cứu |
| D-019 | 2026-07-30 | Accepted | FastAPI chỉ tạo/xếp hàng Flower job; worker riêng đọc DB và chạy subprocess bằng argv cùng profile dữ liệu phía server | không chạy image training dài trong web worker và không cho HTTP truyền shell command/đường dẫn dữ liệu |
| D-020 | 2026-07-30 | Accepted | Canonical FedProx smoke phải có ít nhất hai local optimizer step và so sánh tensor với FedAvg | một batch duy nhất làm proximal gradient bằng 0 tại bước đầu, chỉ chứng minh wiring chứ chưa chứng minh hạng proximal tác động |
| D-021 | 2026-07-30 | Accepted | Giữ artifact history JSON đầy đủ đồng thời chuẩn hóa scalar mỗi round vào `experiment_rounds` | JSON bảo toàn per-class/confusion để tái lập; cột scalar giúp API/dashboard truy vấn ổn định mà không làm mất bằng chứng gốc |
| D-022 | 2026-07-30 | Accepted | Đo communication bằng byte thực của Flower Array/Metric/Config Record theo từng chiều, phase và client; tên phép đo luôn ghi rõ loại trừ transport/TLS overhead | có số đo kiểm chứng được từ payload ứng dụng nhưng không tuyên bố sai là toàn bộ network traffic |
| D-023 | 2026-07-30 | Accepted | Exporter luôn loại artifact tổng hợp hoặc có `research_result_valid=false`; output mới có checksum và environment manifest | ngăn smoke metric lọt vào bảng kết quả nghiên cứu và tăng khả năng truy vết |
| D-024 | 2026-07-30 | Accepted | Ba profile IID/Dirichlet dùng cùng một split train/global-test được tạo trong một lệnh, xác nhận bằng SHA-256 và không ghi đè output cũ | tránh vô tình đổi test set giữa thuật toán và giảm sai thao tác thủ công |
| D-025 | 2026-07-30 | Accepted | Dashboard chỉ nhận class count/proportion từ `partition_summary.json`, không nhận byte ảnh hoặc local path | đáp ứng thống kê/heatmap theo cơ sở mà giữ ranh giới dữ liệu thô tại client |
| D-026 | 2026-07-30 | Accepted | Flower server từ chối model update sai schema, NaN/Inf, zero-sample, duplicate identity hoặc metadata quá giới hạn trước aggregation | áp dụng NFR kiểm tra update tại đúng trust boundary, không chỉ ở lõi NumPy test |
| D-027 | 2026-07-30 | Accepted | Demo Compose bắt buộc dùng secret sinh ngẫu nhiên, bearer token tách vai trò viewer/admin và HTTPS tại Nginx; DB/API không publish port | đóng ranh giới điều khiển local mà không đưa credential vào frontend image; không ngầm coi đây là Flower node auth hay bảo mật production nhiều máy |
| D-028 | 2026-07-31 | Accepted | Flower image run mặc định mang `result_kind=federated_image_pilot` và `research_result_valid=false`; chỉ protocol đã khóa mới được bật cờ nghiên cứu tường minh | ngăn pilot/smoke vô tình lọt vào exporter hoặc bảng kết quả chính |
| D-029 | 2026-07-31 | Accepted | Flower strategy phải fail-fast nếu thiếu bất kỳ valid reply đã gửi; Windows pilot dùng một Ray actor/8 CPU sau khi bốn actor song song chết, còn main study ưu tiên Linux | không aggregate mô hình từ tập reply thiếu và ghi rõ workaround runtime thay vì che lỗi Ray/Windows |
| D-030 | 2026-08-06 | Accepted | Mở rộng phạm vi thuật toán vượt D-007: cài đặt đủ FedAvg, FedProx, FedBN, SCAFFOLD, MOON theo §2/§5 GĐ3 của đề cương. SCAFFOLD truyền control variate `c` qua ArrayRecord riêng và client trả delta `c_i⁺ - c_i`; MOON nhận `μ`/`τ` qua ConfigRecord và giữ model round trước trong `Context.state`. Server **bắt buộc raise** nếu client không gửi đủ trạng thái thuật toán | D-007 chỉ chốt 4 thuật toán cho MVP nhưng code đã cài 5; để log mâu thuẫn với code là nợ tài liệu. Quan trọng hơn: `scaffold`/`moon` trước đây chạy âm thầm y hệt FedAvg vì client không nhận/không gửi trạng thái nào — sinh bảng kết quả trông hợp lệ, khác nhau do nhiễu, nhưng không phản ánh thuật toán được tuyên bố. Fail-fast theo tinh thần D-026/D-029 để lỗi này không thể tái diễn im lặng |
| D-031 | 2026-08-07 | Accepted | FedBN được thực thi **tại client**, không tại aggregation: sau khi nạp trọng số global, client ghi đè batch-norm bằng thống kê BN của chính nó (`Context.state`) rồi mới train — và làm đúng như vậy trước khi validate. Client báo `fedbn_local_bn_tensors`; `TrackedFedBN` raise từ round 2 nếu giá trị bằng 0. `batch_norm_parameter_names` là định nghĩa **duy nhất** về "tensor nào là BN", dùng chung cho cả simulator lẫn Flower client | Trong đường Flower, `fedbn` trước đây là **no-op hoàn toàn**: `fedbn_weighted_average_updates` chỉ gọi được từ `simulation.py`, `TrackedFedBN` là alias trần của `FedAvg`, client không làm gì riêng. Một scenario `fedbn` sẽ sinh ra số của FedAvg dưới nhãn `fedbn` — đúng lỗi D-030 mô tả, lặp lại ở thuật toán thứ ba. Đặt tại client vì server vẫn phải aggregate mọi tensor để checkpoint đầy đủ; loại BN ở server sẽ tạo checkpoint không client nào giữ. Nhánh evaluate cũng phải khôi phục BN: chấm điểm BN trung bình là chấm một model không tồn tại, mà điểm đó chọn checkpoint |
| D-032 | 2026-08-07 | Accepted | Metadata phân hoạch (`partition_kind`, `dirichlet_alpha`, `feature_skew_strength`) đọc từ `profile.json` của chính profile, không suy ra từ tên thư mục. Riêng quantity skew: spec ghi `partition_kind='iid'` + `quantity_skew=true`, launcher chuyển thành `quantity_skew` tường minh. Validator chấp nhận đủ 5 thuật toán và kiểm tra bằng chứng **ở tầng artifact** (`algorithm_artifact_evidence`), không chỉ ở log | `_profile_alpha` cũ suy `"dirichlet" if name != "iid"`: một run quantity-skew sẽ tự ghi `partition_kind="dirichlet", alpha=0.0` vào manifest của mình, mâu thuẫn với `partition_summary.json` và làm hỏng protocol lock. Song song, `parse_flower_log_evidence` chỉ nhận `fedavg`/`fedprox` — run `scaffold` sẽ train hết nhiều giờ GPU rồi fail ở bước cuối. Log chỉ chứng minh strategy nào khởi động; FedBN/SCAFFOLD/MOON đều aggregate y hệt FedAvg ở server nên chỉ metric per-round mới phân biệt được run thật với FedAvg đội lốt |
| D-033 | 2026-08-07 | Accepted | Manifest lưu đường dẫn ảnh **tương đối theo một dataset root cấp lúc chạy** (`--dataset-root` hoặc `CROPFED_DATASET_ROOT`). `resolve_image_path` **raise** khi gặp path tương đối mà không có root, thay vì resolve theo CWD. `dataset_root` là tham số **bắt buộc** của `extend_data_profiles` và của CLI `extend-full-profiles`. `dataset-root` và `num-workers` được **khai báo** trong `[tool.flwr.app.config]` | Path tuyệt đối `F:\project\...` gọi tên một cái máy, không mô tả một thí nghiệm: cùng bộ profile không chạy được trên máy GPU hay trong container. Resolve theo CWD sẽ *im lặng* đọc sai thư mục, nên chọn fail-loud. `dataset_root` từng là optional với docstring nói là tùy chọn — sai: thiếu nó thì audit báo **mọi** ảnh `invalid_image`, trông như dữ liệu hỏng chứ không như thiếu tham số. Khóa `run_config` không khai báo trong pyproject sẽ khiến `run_config.get(...)` lặng lẽ dùng default — đúng lớp lỗi đã ghi ở D-030 |
| D-034 | 2026-08-07 | Accepted | Sáu profile 38-class có sẵn được migrate **tại chỗ** sang path tương đối bằng `scripts/migrate_manifest_paths.py`; tính đúng của dataset root được **chứng minh** bằng bất biến `image_id == sha1(relative_path)[:16]` trên từng dòng, một dòng lệch là hủy toàn bộ trước khi ghi. SHA-256 trong `profile.json`/`profiles_index.json` được cập nhật theo bytes mới; `scripts/rewrite_profile_paths.py` chuyển sang **Superseded** và raise ngay khi chạy | Sáu profile tốn nhiều giờ phân hoạch, làm lại là lãng phí. Rủi ro thật không phải crash mà là rewrite **thành công** theo root sai: manifest vẫn parse sạch nhưng trỏ sang ảnh khác. `image_id` được sinh từ relative path lúc chuẩn bị nên là bằng chứng sẵn có, không phải phỏng đoán — 847.194 dòng, 0 lệch, xác nhận root là `data/raw/PlantVillage-Dataset/raw/color`. Chỉ CSV chứa path nên `partition_summary.json` không bị chạm, giữ nguyên D-025. Script cũ hardcode `/app/data/` và vẫn `replace("\\", "/")`: chạy lại lên bộ đã migrate sẽ làm hỏng manifest, nên chặn cứng thay vì xóa file (còn được `docs/10`, `docs/11` dẫn chiếu) |
| D-035 | 2026-08-07 | Accepted | Protocol lock cho cả 15 scenario được sinh bằng `scripts/generate_protocol_locks.py`, **import lại** `scripts/run_main_study.py` để dùng chung ma trận `SCENARIOS`, hàm đọc metadata profile và default tham số. Lệnh từ chối ghi đè một thư mục lock đã có. Lock phải được sinh **trước** run mà nó ràng buộc | `validate_protocol_lock` so khớp `config` và `manifest_hashes` **chính xác**, nên một lock lệch dù một khóa cũng chỉ bị phát hiện ở bước validate cuối — nghĩa là sau khi đã đốt xong thời gian GPU. Viết lại config ở nơi thứ hai là cách chắc chắn để nó lệch; import runner khiến một thay đổi trong study không thể âm thầm vô hiệu hóa toàn bộ lock. Hai cái bẫy đã được test khóa lại: profile IID phải khóa `dirichlet_alpha = 0.0` (không phải `null`, vì runner ghi `alpha or 0.0` rồi server cast `float`), và cả bốn hyperparameter thuật toán phải có mặt với giá trị 0 khi không áp dụng. Sinh lock từ output của một run đã xong thì không chứng minh điều gì, nên cấm ghi đè |

## Mẫu entry mới

```text
ID:
Ngày:
Trạng thái: Proposed | Accepted | Superseded
Bối cảnh:
Quyết định:
Các phương án đã cân nhắc:
Hệ quả:
Files/tests cần cập nhật:
```
